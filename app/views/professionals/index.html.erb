<div class="title-full">
  <div class="container">
    <h1>Find Professionals</h1>
  </div>
</div>
<div class="container">
  <div class="professional-cards">
    <% @professionals.each do |professional| %>
      <%= link_to professional_path(professional) do  %>
        <div class="professional-card">
          <% if professional.photo.attached? %>
            <%= cl_image_tag professional.photo.key %>
          <% else %>
            <i class="fas fa-user-circle user-avatar"></i>
          <% end %>
          <div class="professional-info">
            <h5><%= professional.full_name %></h5>
            <h6><%= professional.role %></h6>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>